[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "python manage.py collectstatic --noinput"
]

[phases.deploy]
cmds = [
  "python manage.py migrate",
  "python manage.py poblar_db"
]

[start]
cmd = "gunicorn ve_academy.wsgi --bind 0.0.0.0:$PORT --workers 2"
